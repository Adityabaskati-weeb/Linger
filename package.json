{
  "name": "campusiq",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native campus operating system for students, faculty, and administrators.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w @campusiq/backend\" \"npm run dev -w @campusiq/frontend\"",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:smoke": "node scripts/smoke-test.mjs",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
